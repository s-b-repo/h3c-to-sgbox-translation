# H3C to SGBox Log Translator — Debian 13 (Trixie)
# All dependencies actively maintained as of March 2026
# ─────────────────────────────────────────────────────
aiohttp>=3.13,<4.0          # Async HTTP server (replaces stdlib http.server)
uvloop>=0.22                # High-performance asyncio event loop
structlog>=25.1             # Structured/JSON logging
tenacity>=9.0               # Retry logic with exponential backoff
pyOpenSSL>=25.0             # Enhanced TLS/SSL support
python-gnupg>=0.5.6         # GPG encryption (SGBox at-rest log encryption)
